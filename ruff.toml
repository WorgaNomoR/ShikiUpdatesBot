line-length = 100

[lint]
# Дефолт ruff (E4/E7/E9 + F = pyflakes: unused imports/undefined names) + сортировка
# импортов (I). E501 намеренно не включаем — стиль проекта длиннее 88
select = ["E4", "E7", "E9", "F", "I"]
